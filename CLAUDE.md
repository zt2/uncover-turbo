# CLAUDE.md

本文件为 Claude Code / AI 助手在本仓库工作时提供指引。**其中「规约驱动开发（SDD）红线」为不可绕过的硬性规则,任何代码改动都必须遵守。**

## 项目简介

`uncover-turbo` 是一个基于 project-discovery [uncover](https://github.com/projectdiscovery/uncover) 改造的 Go 命令行工具:通过 LLM 把自然语言翻译成各测绘搜索引擎（FOFA / 360 Quake / Censys / ZoomEye）的查询语法,再交给 uncover 执行搜索。

### 代码结构

- `cmd/uncover-turbo/main.go` —— 程序入口。解析参数,对每个引擎的自然语言输入调用翻译,再构造 runner 执行搜索。
- `pkg/bot/bot.go` —— 封装 OpenAI 客户端,`Translate(engine, text)` 把自然语言翻译成对应引擎语法。
- `pkg/bot/prompt.go` —— 各引擎的翻译提示词（system prompt）。
- 依赖 `github.com/projectdiscovery/uncover` 的 `runner` 包驱动实际搜索。

### 关键约束

- 运行需环境变量 `OPENAI_KEY`。
- `runner.NewRunner` 会在创建时把各引擎的查询快照进内部 service,因此 **必须先完成翻译、再创建 runner**;不要在翻译前构造 runner。

## 常用命令

```bash
go build ./...        # 构建
go vet ./...          # 静态检查
go test ./...         # 测试
go mod tidy           # 整理依赖
```

---

## 🔴 规约驱动开发（SDD）红线 —— 硬性规则,不可绕过

**任何功能开发、缺陷修复或重构,都必须严格按以下四个阶段顺序推进。上一阶段的产出文档未落地,不得进入下一阶段;严禁在没有规约的情况下直接写实现代码。**

规约文档统一存放在 `specs/<功能名>/` 目录下,四个阶段各自对应一个文件。

### 阶段一 · Spec（规约）→ `specs/<功能名>/spec.md`

先写清楚「做什么、为什么」,不涉及实现细节。至少包含:
- **需求背景 / 目标**:要解决的问题。
- **验收标准**:可验证的成功条件（尽量可测量、可复现）。
- **影响面**:涉及的模块、外部依赖、兼容性与安全影响。
- **非目标**:明确本次不做什么。

> 🚫 未产出并确认 `spec.md`,不得进入阶段二。

### 阶段二 · Plan（方案）→ `specs/<功能名>/plan.md`

基于已确认的 spec 设计「怎么做」。至少包含:
- **技术方案**:改动的文件 / 数据流 / 接口设计。
- **关键决策与取舍**:为什么这样做,备选方案及放弃理由。
- **风险点**:可能的破坏性变更、回滚策略。

> 🚫 未产出并确认 `plan.md`,不得进入阶段三。

### 阶段三 · Tasks（任务拆解）→ `specs/<功能名>/tasks.md`

把方案拆成有序、可独立验证的小任务。每条任务需:
- 描述具体动作与对应的验收点。
- 标注依赖关系与顺序。
- 保持粒度足够小,可单独提交与回滚。

> 🚫 未产出并确认 `tasks.md`,不得进入阶段四。

### 阶段四 · Implement（实现）

严格按 `tasks.md` 的顺序逐条实现。要求:
- 每条任务完成后运行 `go build ./...` 与 `go vet ./...`（有测试则加 `go test ./...`）验证。
- 实现若偏离 spec / plan,**先回头更新对应文档**,再继续编码——文档与代码必须始终一致。
- 提交信息需能对应到具体任务与规约。

### 红线要点（务必牢记）

1. **顺序不可逆**:Spec → Plan → Tasks → Implement,禁止跳阶段。
2. **文档先行**:每个阶段先落地文档并确认,再往下走。
3. **无规约不编码**:任何实现代码都必须能追溯到已确认的 spec。
4. **文档随代码同步**:实现中的变更要反向更新规约文档,严禁文档与代码脱节。
